# Research boundary

The existing R strategy and quantitative research code is retained for research/backtesting. Production requests must not depend on an R runtime.

Use research outputs as versioned strategy specifications and test fixtures before implementing production analytics in Python.
